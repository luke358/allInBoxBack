<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luke.allinboxback.mapper.UserAppMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.luke.allinboxback.entity.UserApp">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="app_id" property="appId" />
        <result column="sorted" property="sorted" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, app_id, enable, isNotificationEnabled, isSoundsEnabled, isShowNameInTabEnabled, isHibernateEnabled, isHibernating, isUnreadInTabEnabled, isUnreadInGlobalEnabled, preload, timer, lastUsed, lastHibernated, isActive, isMuted, isFirstLoad, isError, isLoading, isMediaPlaying, linkHandling
    </sql>

</mapper>
